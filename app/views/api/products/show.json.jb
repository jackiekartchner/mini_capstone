 {
    id: @product.id,
    name: @product.name,
    price: @product.price,
    tax: @product.tax,
    total: @product.total,
    image_url: @product.image_url,
    description: @product.description,
    is_discounted?: @product.is_discounted?
  }